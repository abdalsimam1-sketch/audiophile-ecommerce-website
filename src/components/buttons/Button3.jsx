import React from "react";

export const Button3 = ({ text }) => {
  return (
    <button className="btn button3">
      {text}
      <i className="bi bi-arrow button-icon"></i>
    </button>
  );
};
